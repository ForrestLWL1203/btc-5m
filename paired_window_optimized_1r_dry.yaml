# One-round dry-run verification config derived from paired_window_optimized.yaml

market:
  asset: btc
  timeframe: 5m

rounds: 1

strategy:
  type: paired_window
  theta_pct: 0.02
  entry_start_remaining_sec: 240
  entry_end_remaining_sec: 60
  persistence_sec: 10
  max_entry_price: 0.65
  min_move_ratio: 0.7
  open_price_max_wait_sec: 30

params:
  amount: 1.0
  max_entries_per_window: 1
